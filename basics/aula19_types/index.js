// symbol
let ablublue; // undefined -> não aponta para local algum na memória (eu não posso escolher deixar a variável assim)
const creoQueNo = null; // null -> não aponta para local algum na memória
const name = 'Bruno'; // string
const age = 25; //number
const height = 1.85; //number
const boolean = true; //boolean -> true or false